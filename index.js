let count = 0;

function add() {
  count += 1;
  document.getElementById("contado").textContent = count;
}

function subtract() {
  count -= 1;
  document.getElementById("contado").textContent = count;
}
